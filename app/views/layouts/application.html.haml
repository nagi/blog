!!!
%head
  %title
    = h(yield(:title) || "Nagi Systems Ltd")
  = stylesheet_link_tag 'application'
  = stylesheet_link_tag 'jquery.lightbox-0.5'
  = javascript_include_tag :defaults
  = javascript_include_tag 'jquery.lightbox-0.5'
  = yield(:head)
%body
  #site_container
    = render :partial => 'global/navigation_bar', :locals => {:title => yield(:title)}
    #shadow
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"
      #content
        = yield
      #footer
        #footer_content
          %ul
            %li= link_to 'Home', home_url
            %li= link_to 'Portfolio', portfolio_url
            %li= link_to 'Contact', contact_url
            %li= link_to 'Blog', blog_url
